import { FfValidationDirective } from './ff-validation.directive';

describe('FfValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FfValidationDirective();
    expect(directive).toBeTruthy();
  });
});
